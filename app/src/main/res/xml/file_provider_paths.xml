<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- getExternalFilesDir(null) — où AppUpdateDownloader enregistre taoukit_update.apk -->
    <external-files-path name="updates" path="." />
    <!-- getCacheDir()/camera_captures/ — photo prise via l'appareil photo système
         (onShowFileChooser, MainActivity), le temps d'être lue/uploadée par la WebView -->
    <cache-path name="camera_captures" path="camera_captures/" />
</paths>
