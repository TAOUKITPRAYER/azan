# Build outputs
build/
app/build/
app/release/
hijri-admin/build/
*.apk
*.apk.idsig
*.aab

# Gradle
.gradle/
gradle-app.setting

# Local config (machine-specific, never portable — never commit)
local.properties
app/local.properties

# Android Studio / IntelliJ
.idea/
*.iml
.kotlin/
captures/
.cxx/

# Signing (secret — never commit)
tawkit.jks
*.jks
*.keystore

# OS cruft
.DS_Store
Thumbs.db

# Third-party Mushaf APK/pages used only for local, private data analysis —
# never to be committed/redistributed (copyrighted publisher content).
app/mushaf/

# Local reciter-download cache (raw audio archives, 100s of MB — exceeds
# GitHub's file size limit and shouldn't be versioned; downloaded at
# runtime by ReciterDownloader instead).
reciters-catalog/*.zip
